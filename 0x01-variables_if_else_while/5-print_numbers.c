#include <stdio.h>
/**
* main - display numbers to stdout
* Description: prints single digits of base 10
* Return: always return 0 (successful)
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
		putchar('\n');
		return (0);
}
