#include <stdio.h>

/**
* _isdigit - checks for digits
* @c: operand
* Return: always 0 (successful)
*/
int _isdigit(int c)
{
	while ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'A'))
	{
		return (0);
	}
		return (1);
	return (1);
}

