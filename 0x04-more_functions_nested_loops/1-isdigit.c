#include <stdio.h>

/**
* _isdigit - checks for digits
* @c: operand
* Return: always 0 (successful)
*/
int _isdigit(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (0);
	}
		return (1);
	return (1);
}

