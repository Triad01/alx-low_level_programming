#include "main.h"
/**
* int _sqrt_recursion - prints the square root of a number
* @c: argument
* Return: always 0 (successful)
*/
int _sqrt_recursion(int n)
{
	if (n < 1)
		return (1);
	else 
		n * _sqrt_recursion(x-1);

}
